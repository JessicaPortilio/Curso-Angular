import { FocusSecondInputDirective } from './focus-second-input.directive';

describe('FocusSecondInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusSecondInputDirective();
    expect(directive).toBeTruthy();
  });
});

import { ListenerDirectiveDirective } from './listener-directive.directive';

describe('ListenerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

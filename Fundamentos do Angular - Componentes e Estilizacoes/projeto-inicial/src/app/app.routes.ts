// Importa o tipo "Routes" do Angular, que é usado para definir as rotas da aplicação.
// As rotas indicam quais páginas ou componentes devem ser exibidos para cada URL.
import { Routes } from '@angular/router';

// Cria uma constante chamada "routes" que armazenará todas as rotas da aplicação.
// Inicialmente, é uma lista vazia, mas será preenchida com as configurações das rotas conforme necessário.
export const routes: Routes = [];

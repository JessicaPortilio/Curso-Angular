// Interface minimalista para representar o usuário retornado pela Promise (apenas campos essenciais)
export interface IUser {
    id: number;
    name: string;
    username: string;
    email: string;
}
